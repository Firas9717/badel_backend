# ── Server Config ──
NODE_ENV=development
PORT=5000

# ── MongoDB Config ──
MONGO_URI=

# ── JWT Config ──
JWT_SECRET=
JWT_EXPIRE=24h

# ── Cookie Config ──
COOKIE_SECURE=false

# ── CORS Config ──
CLIENT_URL=http://localhost:8080

# ── Upload Limits ──
MAX_FILE_SIZE=5242880
MAX_FILES=5
